<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:EventManager.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:EventManager.Views"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="EventManager.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Name="window"
        Title="EventManager">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>
    <!-- <ScrollViewer> -->
    <Grid RowDefinitions="Auto, *" ColumnDefinitions="Auto,*">
        <Button Grid.Row="0" Grid.Column="0" Name="LoadButton" Command="{Binding Load}">Load</Button>
        <views:EventsList Grid.Row="1" Grid.Column="0"
                          Name="EventsList"
                          Margin="0 10"
                          DataContext="{Binding EventsList}" />
        <views:EventEditor Grid.Row="1" Grid.Column="1"
                           Margin="10"
                           DataContext="{Binding EventsList.EditorViewModel^}" />
    </Grid>
    <!-- </ScrollViewer> -->

</Window>
